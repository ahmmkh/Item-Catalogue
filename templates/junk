<div class="row">
  <div class="col-xs-12 col-md-9">

    <div class="section-container-spacer">
      <h1>{{category.name}}</h1>
    </div>

  {% for category_item in category_items %}
    <div class="section-container-spacer">
      <h3>{{category_item.name}} - {{category_item.price}}</h3>
      <p><img class="img-responsive" alt="" src="/static/{{category_item.picture}}"></p>
      <p>{{category_item.description}}</p>
      <br>
    </div>
  {% endfor %}
  </div>

</div>
